// var yo = require('yo-yo')
//
// function takePicture (state) {
//   var cameraInput = document.getElementById('cameraInput')
//   function sendPic() {
//       var file = myInput.files[0]
//       //Send file here either by adding it to a FormData obj and sending that via XHR, or by passing the file into the 'send' method of an XHR instance.
//   }
//   cameraInput.addEventListener('change', sendPic, false)
//   return yo `
//   <div>
//     <input id="myFileInput" type="file" accept="image/*; capture=camera"/>
//   </div>
//   `
// }
//
// module.exports = header
//
//
//
// <input id="myFileInput" type="file" accept="image/*; capture=camera"/>
// var cameraInput = document.getElementById('cameraInput')
// function sendPic() {
//     var file = myInput.files[0]
//     //Send file here either by adding it to a FormData obj and sending that via XHR, or by passing the file into the 'send' method of an XHR instance.
// }
// capture.addEventListener('change', sendPic, false)
